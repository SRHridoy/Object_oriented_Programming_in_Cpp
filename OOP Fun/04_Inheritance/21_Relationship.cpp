/*Relationship---->
                (i)isA Relationship
                (isA = Inheritance)

                (ii)hasA Relationship
                (hasA = Object)

    isA Relationship                hasA Relationship
-------------------------------------------------------
    (i)isA relationship             (i)hasA relationship is
    is based on Inheritance.        based on Objects.

    (ii)isA relationship expose     (ii)hasA relationship,
    all public data of base         use public data of 
    classes.                        derived class.

    (iii)isA relationship is        (iii)hasA relationship 
    static binding(compile time).   dynamic binding(run time).

    (iv)isA relationship used when  (iv)haA relationship use 
    can actually inherit.           when we can't inherit
    (person----teacher)             something...
    (person----dress)   


*/